"use client";

import React from 'react';
import { useRouter } from 'next/navigation';

function LandingPage() {
  const router = useRouter();
  // ... rest of the component, replace navigate('/chat') with router.push('/chat') ...
}

export default LandingPage; 